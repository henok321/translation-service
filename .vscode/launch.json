{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run locally",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "preLaunchTask": "make setup",
      "postDebugTask": "docker-compose down",
      "program": "${workspaceFolder}/cmd/main.go",
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
